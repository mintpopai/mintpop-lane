package ai.mintpop.lane.mapper;

import ai.mintpop.lane.entity.OAuthSession;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;

/** oauth_session 表的 SQL 层。CRUD 由 BaseMapper 提供，不写 XML。 */
public interface OAuthSessionMapper extends BaseMapper<OAuthSession> {
}
