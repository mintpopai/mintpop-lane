# JetBrains 工程文件
.idea/

# Claude Code 本地配置
.claude/

# 系统文件
.DS_Store

# 依赖与构建产物
node_modules/
dist/
target/

# 本地环境覆盖文件
*.local

# 服务端配置（含数据库口令与加密密钥，严禁入库；模板见同目录 application.example.yml）
apps/server/config/application.yml

# 部署机上的服务端配置（含凭据，放仓库根、与 docker-compose.yml 同目录）
/application.yml

# 部署机上的环境变量文件（可选，覆盖镜像版本/端口/时区）
/.env

# Superpowers SDD 工作区（临时产物，不入库）
.superpowers/
