# 客户端引导配置下发 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把桌面端登录所需的 Logto issuer / client_id / api_resource 从编译期常量改为服务端匿名端点下发，客户端只保留 `MINTPOP_SERVER_URL` 一个编译期常量。

**Architecture:** 服务端新增匿名端点 `GET /api/client-config`，其中 issuer 与 apiResource 复用资源服务器的鉴权配置（`OAuth2ResourceServerProperties`），只有 client_id 是新配置。桌面端在 Tauri `setup` 阶段先拉取该配置存入 `AppState`，成功后才尝试静默登录；拉不到就发事件让登录页显示重试按钮，不做本地缓存回退。

**Tech Stack:** Spring Boot 4.1（Java 25）、MyBatis-Plus、JUnit 5 + MockMvc + Testcontainers；Tauri 2 + Rust 1.97 + reqwest；Vue 3 + Vite。

**Spec:** `docs/superpowers/specs/2026-08-19-客户端引导配置下发-design.md`

## Global Constraints

- **注释与文档一律中文**；代码标识符、命令、API 名保持英文。
- **统一返回体**：所有接口返回 `ApiResponse<T>`，`code=0` 为成功，HTTP 一律 200（鉴权中间件的 401 除外）。
- **依赖注入**：Java 侧一律构造器注入，不用字段注入、不在方法里 new 依赖。
- **不新增 `BizCodeEnum` 条目**：本期的失败都不是业务错误。
- **命令一律走 mise**：`mise run test-server`、`mise run test-desktop`、`mise run lint-desktop`、`mise run build-desktop`。
- **应用标识**：本期统一为 `ai.mintpop.cli`（Task 4）。
- **`MINTPOP_SERVER_URL` 缺省值保持 `http://127.0.0.1:8080`**，不得改动——本地自测包靠它区分环境。
- **`redirect_uri` 固定 `mintpop://callback`**，由客户端持有，服务端不下发。
- 提交信息用中文、遵循约定式提交前缀（`feat:` / `docs:` / `ci:`）。

---

### Task 1: 服务端下发引导配置

**Files:**
- Create: `apps/server/src/main/java/com/mintpop/server/config/ClientProperties.java`
- Create: `apps/server/src/main/java/com/mintpop/server/response/ClientConfigResponse.java`
- Create: `apps/server/src/main/java/com/mintpop/server/controller/ClientConfigController.java`
- Modify: `apps/server/src/main/java/com/mintpop/server/config/SecurityConfig.java`
- Modify: `apps/server/src/main/resources/application-local.yaml.example`
- Modify: `apps/server/src/main/resources/application-prod.yaml.example`
- Modify: `apps/server/src/test/resources/application-test.yaml`
- Test: `apps/server/src/test/java/com/mintpop/server/controller/ClientConfigControllerTest.java`

**Interfaces:**
- Consumes: 既有的 `ApiResponse.success(T)`、`MysqlTestBase`（`@SpringBootTest` + `@ActiveProfiles("test")` + MySQL 容器单例）。
- Produces: HTTP 契约 `GET /api/client-config` → `{"code":0,"data":{"logtoIssuer":String,"logtoClientId":String,"apiResource":String},"msg":null}`，Task 2 的桌面端按此反序列化。

- [ ] **Step 1: 给测试配置补上要下发的三个值**

在 `apps/server/src/test/resources/application-test.yaml` 的 `jwk-set-uri` 之下追加 `issuer-uri` 与 `audiences`，并在 `mintpop:` 下追加 `client` 段。改完整个文件是：

```yaml
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          # 用 jwk-set-uri 而非 issuer-uri：后者会在应用启动时拉 OIDC discovery，
          # 让测试依赖外网。jwk-set-uri 是懒加载，而 MockMvc 的 jwt() 后处理器
          # 直接注入认证对象、不走真实解码，因此这个地址永远不会被真的请求。
          jwk-set-uri: "http://127.0.0.1:9/jwks"
          # 下面两项只作为「下发给客户端的数据」被读取，不参与解码器构建：
          # Spring Boot 的 IssuerUriCondition 在 jwk-set-uri 有值时直接判定不匹配，
          # 故这里配了 issuer-uri 也不会触发启动期的 OIDC discovery。
          issuer-uri: "https://tenant.test.example/oidc"
          audiences:
            - "https://api.mintpop.test"

mintpop:
  link:
    # 特意与 LinkProperties.ttlSeconds 的默认值（1800）不同：若把配置绑定删掉，
    # 字段会回落到默认值，测试断言必须能因此变红，才守得住「配置真的生效」
    ttl-seconds: 900
  client:
    # 同理：测试值与生产值不同，配置绑定被删时断言必须变红
    logto-client-id: "test-client-id"
  crypto:
    # 测试专用固定密钥（32 字节的 Base64），只用于测试，不是任何环境的真实密钥
    key: "bWludHBvcC10ZXN0LWtleS0wMTIzNDU2Nzg5YWJjZCE="
```

- [ ] **Step 2: 写失败的测试**

创建 `apps/server/src/test/java/com/mintpop/server/controller/ClientConfigControllerTest.java`：

```java
package com.mintpop.server.controller;

import com.mintpop.server.support.MysqlTestBase;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.webmvc.test.autoconfigure.AutoConfigureMockMvc;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

/**
 * 本用例不需要数据库，继承 MysqlTestBase 只是为了复用同一个 Spring 测试上下文缓存，
 * 避免因配置不同而额外启一份上下文。
 */
@AutoConfigureMockMvc
class ClientConfigControllerTest extends MysqlTestBase {

    @Autowired
    private MockMvc mockMvc;

    @Test
    @DisplayName("匿名可拿到引导配置，三个字段与配置一致")
    void 匿名可拿到引导配置() throws Exception {
        mockMvc.perform(get("/api/client-config"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.code").value(0))
                .andExpect(jsonPath("$.data.logtoIssuer").value("https://tenant.test.example/oidc"))
                .andExpect(jsonPath("$.data.logtoClientId").value("test-client-id"))
                // 与鉴权配置的 audiences[0] 同源：改鉴权配置，下发值必须跟着变
                .andExpect(jsonPath("$.data.apiResource").value("https://api.mintpop.test"));
    }

    @Test
    @DisplayName("放行只波及该端点，链路配置匿名访问仍被拒")
    void 放行不波及其它端点() throws Exception {
        mockMvc.perform(get("/api/link/config"))
                .andExpect(status().isUnauthorized());
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `mise run test-server`
Expected: `匿名可拿到引导配置` 失败（端点不存在，被 `anyRequest().denyAll()` 拒掉，实际状态是 401/403 而非 200）。`放行不波及其它端点` 此时应当已经通过。

- [ ] **Step 4: 新增配置属性类**

创建 `apps/server/src/main/java/com/mintpop/server/config/ClientProperties.java`：

```java
package com.mintpop.server.config;

import lombok.Data;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.stereotype.Component;

/**
 * 桌面端引导配置里本服务自有的参数。
 * issuer 与 API Resource 不在这里重复声明：它们复用资源服务器的鉴权配置，
 * 保证客户端拿到的 resource 与服务端校验的 aud 必然是同一个值。
 */
@Data
@Component
@ConfigurationProperties(prefix = "mintpop.client")
public class ClientProperties {

    /** Logto 中桌面端那个「原生应用」的 App ID */
    private String logtoClientId;
}
```

- [ ] **Step 5: 新增响应体**

创建 `apps/server/src/main/java/com/mintpop/server/response/ClientConfigResponse.java`：

```java
package com.mintpop.server.response;

/**
 * 下发给桌面端的登录接入配置。字段名与客户端 ClientConfigData 逐字对应。
 * 三个值均为公开信息，故该端点匿名可访问。
 */
public record ClientConfigResponse(
        String logtoIssuer,
        String logtoClientId,
        String apiResource
) {
}
```

- [ ] **Step 6: 新增控制器**

创建 `apps/server/src/main/java/com/mintpop/server/controller/ClientConfigController.java`。注意 `OAuth2ResourceServerProperties` 在 Spring Boot 4 里的包名是 `org.springframework.boot.security.oauth2.server.resource.autoconfigure`（不是旧的 `org.springframework.boot.autoconfigure.security.oauth2.resource`），它由 `OAuth2ResourceServerAutoConfiguration` 上的 `@EnableConfigurationProperties` 注册，可直接注入：

```java
package com.mintpop.server.controller;

import com.mintpop.server.config.ClientProperties;
import com.mintpop.server.response.ApiResponse;
import com.mintpop.server.response.ClientConfigResponse;
import org.springframework.boot.security.oauth2.server.resource.autoconfigure.OAuth2ResourceServerProperties;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

/**
 * 客户端引导配置：回答「怎么登录」。
 * 与 /api/link/config（回答「登录后给你什么」）分工明确——前者匿名，后者必须带 JWT。
 * 这里下发的三个值都是公开信息（公共客户端的 client_id 本就会出现在浏览器地址栏），
 * 真正的秘密只经 /api/link/config 下发。
 */
@RestController
public class ClientConfigController {

    private final ClientProperties clientProperties;
    private final OAuth2ResourceServerProperties resourceServerProperties;

    public ClientConfigController(ClientProperties clientProperties,
                                  OAuth2ResourceServerProperties resourceServerProperties) {
        this.clientProperties = clientProperties;
        this.resourceServerProperties = resourceServerProperties;
    }

    @GetMapping("/api/client-config")
    public ApiResponse<ClientConfigResponse> clientConfig() {
        OAuth2ResourceServerProperties.Jwt jwt = resourceServerProperties.getJwt();
        List<String> audiences = jwt.getAudiences();
        // 客户端拿它当授权请求的 resource 参数，服务端用同一个值校验 aud，一份配置派生两处
        String apiResource = (audiences == null || audiences.isEmpty()) ? null : audiences.get(0);

        return ApiResponse.success(new ClientConfigResponse(
                jwt.getIssuerUri(),
                clientProperties.getLogtoClientId(),
                apiResource));
    }
}
```

- [ ] **Step 7: 放行该端点**

修改 `apps/server/src/main/java/com/mintpop/server/config/SecurityConfig.java` 的 `authorizeHttpRequests` 块，在 `/api/**` 那条之前插入一行（顺序错了会被通配规则吃掉）：

```java
                .authorizeHttpRequests(auth -> auth
                        .requestMatchers(HttpMethod.GET, "/actuator/health").permitAll()
                        // 引导配置匿名可取：客户端还没登录时就要用它去 Logto 授权
                        .requestMatchers(HttpMethod.GET, "/api/client-config").permitAll()
                        // 管理端要求库里的角色是 ADMIN，规则必须排在下面那条通配之前
                        .requestMatchers("/api/admin/**").hasAuthority("ROLE_ADMIN")
                        .requestMatchers("/api/**").authenticated()
                        // 未显式放行的路径一律拒绝，避免将来新增端点时忘记加规则
                        .anyRequest().denyAll())
```

- [ ] **Step 8: 跑测试确认通过**

Run: `mise run test-server`
Expected: PASS，两条用例都绿。

- [ ] **Step 9: 补配置模板**

在 `apps/server/src/main/resources/application-local.yaml.example` 的 `mintpop:` 段里，`link:` 之后、`crypto:` 之前插入：

```yaml
  client:
    # Logto 中桌面端那个「原生应用」的 App ID，会随引导配置下发给桌面端
    logto-client-id: 填入 Logto 里那个原生应用的 App ID
```

在 `apps/server/src/main/resources/application-prod.yaml.example` 的 `mintpop:` 段里，`link:` 之后插入同样的两行（注释一致）。

- [ ] **Step 10: 手工验证端点**

Run: `mise run run-server`（另开终端）后执行
```bash
curl -s http://127.0.0.1:8080/api/client-config
```
Expected: 返回 `code` 为 0、`data` 里三个字段与本地 `application-local.yaml` 一致。验证完停掉服务端。

- [ ] **Step 11: 提交**

```bash
git add apps/server/src/main/java/com/mintpop/server/config/ClientProperties.java \
        apps/server/src/main/java/com/mintpop/server/response/ClientConfigResponse.java \
        apps/server/src/main/java/com/mintpop/server/controller/ClientConfigController.java \
        apps/server/src/main/java/com/mintpop/server/config/SecurityConfig.java \
        apps/server/src/main/resources/application-local.yaml.example \
        apps/server/src/main/resources/application-prod.yaml.example \
        apps/server/src/test/resources/application-test.yaml \
        apps/server/src/test/java/com/mintpop/server/controller/ClientConfigControllerTest.java
git commit -m "feat: 服务端下发桌面端登录引导配置"
```

---

### Task 2: 桌面端引导配置拉取模块

**Files:**
- Create: `apps/desktop/src-tauri/src/auth/bootstrap.rs`
- Modify: `apps/desktop/src-tauri/src/auth/mod.rs`

**Interfaces:**
- Consumes: Task 1 的 HTTP 契约；既有的 `crate::link::remote::{ApiResponse, RemoteError}` 与 `crate::auth::oidc::OidcConfig`。
- Produces: `pub const REDIRECT_URI: &str`、`pub async fn fetch_client_config(base_url: &str) -> Result<OidcConfig, RemoteError>`、`pub struct ClientConfigData`（带 `into_oidc_config()`）。Task 3 只用前两个。

- [ ] **Step 1: 写失败的测试与模块骨架**

创建 `apps/desktop/src-tauri/src/auth/bootstrap.rs`，先只写类型定义与测试（实现留到 Step 3）：

```rust
use crate::auth::oidc::OidcConfig;
use crate::link::remote::{ApiResponse, RemoteError};
use serde::Deserialize;
use std::time::Duration;

const REQUEST_TIMEOUT: Duration = Duration::from_secs(15);

/// 桌面端注册的 deep link 回调地址。它由客户端自己的 scheme 决定，服务端不下发。
pub const REDIRECT_URI: &str = "mintpop://callback";

/// 服务端下发的登录接入配置。字段名是 Java 侧的驼峰形式。
/// 三个字段都不是 Option：任一缺失都说明服务端配置有问题，应当当场报错，
/// 而不是拿空串去拼一个必然失败的授权地址。
#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ClientConfigData {
    pub logto_issuer: String,
    pub logto_client_id: String,
    pub api_resource: String,
}

impl ClientConfigData {
    pub fn into_oidc_config(self) -> OidcConfig {
        OidcConfig {
            issuer: self.logto_issuer,
            client_id: self.logto_client_id,
            redirect_uri: REDIRECT_URI.to_string(),
            resource: self.api_resource,
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn 业务码为零时映射成登录配置() {
        let raw = r#"{"code":0,"data":{"logtoIssuer":"https://tenant.logto.app/oidc","logtoClientId":"client-1","apiResource":"https://api.mintpop.internal"},"msg":null}"#;
        let resp: ApiResponse<ClientConfigData> = serde_json::from_str(raw).unwrap();

        let cfg = resp.into_data().unwrap().into_oidc_config();

        assert_eq!(cfg.issuer, "https://tenant.logto.app/oidc");
        assert_eq!(cfg.client_id, "client-1");
        assert_eq!(cfg.resource, "https://api.mintpop.internal");
        // redirect_uri 由客户端持有，与服务端下发的内容无关
        assert_eq!(cfg.redirect_uri, "mintpop://callback");
    }

    #[test]
    fn 业务码非零时转为错误并带上服务端文案() {
        // HTTP 是 200，成败只看 code——与服务端约定一致
        let raw = r#"{"code":110002,"data":null,"msg":"服务内部错误"}"#;
        let resp: ApiResponse<ClientConfigData> = serde_json::from_str(raw).unwrap();

        match resp.into_data() {
            Err(RemoteError::Biz { code, msg }) => {
                assert_eq!(code, 110002);
                assert_eq!(msg, "服务内部错误");
            }
            other => panic!("期望业务错误，实际是 {other:?}"),
        }
    }

    #[test]
    fn 缺字段的返回体解析失败而不是静默用空串() {
        // 服务端漏配 logto-client-id 时会下发 null，此处必须报错
        let raw = r#"{"code":0,"data":{"logtoIssuer":"https://tenant.logto.app/oidc","logtoClientId":null,"apiResource":"https://api.mintpop.internal"},"msg":null}"#;

        let parsed: Result<ApiResponse<ClientConfigData>, _> = serde_json::from_str(raw);

        assert!(parsed.is_err());
    }
}
```

在 `apps/desktop/src-tauri/src/auth/mod.rs` 顶部加一行（保持字母序）：

```rust
pub mod bootstrap;
pub mod oidc;
pub mod pkce;
pub mod storage;
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mise run test-desktop`
Expected: 编译失败——`REQUEST_TIMEOUT` 未被使用（`unused` 警告不致命），但 `ApiResponse` 的 `into_data` 需要 `RemoteError`；真正的失败点是 `业务码为零时映射成登录配置` 里 `OidcConfig` 字段可见性/派生若不满足会报错。若编译通过则三条用例应当直接绿——那说明本步只是类型定义，属正常；继续 Step 3 补拉取函数。

- [ ] **Step 3: 补上拉取函数**

在 `apps/desktop/src-tauri/src/auth/bootstrap.rs` 的 `impl ClientConfigData` 之后、`#[cfg(test)]` 之前插入：

```rust
fn client() -> reqwest::Client {
    // 引导发生在链路建立之前，只能走用户自己的网络，因此不套本地代理
    reqwest::Client::builder()
        .no_proxy()
        .timeout(REQUEST_TIMEOUT)
        .build()
        .expect("构建 HTTP 客户端不应失败")
}

/// 拉取登录接入配置。这是客户端启动后的第一个网络请求，失败则无法登录。
pub async fn fetch_client_config(base_url: &str) -> Result<OidcConfig, RemoteError> {
    let resp = client()
        .get(format!("{base_url}/api/client-config"))
        .send()
        .await
        .map_err(RemoteError::Request)?;

    if !resp.status().is_success() {
        return Err(RemoteError::Status(resp.status()));
    }

    let body: ApiResponse<ClientConfigData> = resp.json().await.map_err(RemoteError::Request)?;
    Ok(body.into_data()?.into_oidc_config())
}
```

- [ ] **Step 4: 跑测试与 lint 确认通过**

Run: `mise run test-desktop && mise run lint-desktop`
Expected: 三条用例 PASS，clippy 无 warning（`-D warnings` 下 warning 即失败）。

- [ ] **Step 5: 提交**

```bash
git add apps/desktop/src-tauri/src/auth/bootstrap.rs apps/desktop/src-tauri/src/auth/mod.rs
git commit -m "feat: 桌面端新增引导配置拉取模块"
```

---

### Task 3: 桌面端改用下发配置

**Files:**
- Modify: `apps/desktop/src-tauri/src/app_state.rs`
- Modify: `apps/desktop/src-tauri/src/lib.rs`
- Modify: `apps/desktop/src-tauri/src/commands.rs`
- Modify: `apps/desktop/src-tauri/build.rs`
- Modify: `apps/desktop/src/components/Login.vue`

**Interfaces:**
- Consumes: Task 2 的 `auth::bootstrap::fetch_client_config`。
- Produces: Tauri command `reload_client_config() -> Result<(), String>`、`client_config_ready() -> bool`；前端事件 `auth://config-ready`（无 payload）与 `auth://config-failed`（payload `{ reason: string }`）。

- [ ] **Step 1: 在 AppState 里放配置**

修改 `apps/desktop/src-tauri/src/app_state.rs`：`use` 块加 `use crate::auth::oidc::OidcConfig;`，结构体里在 `access_token` 之前插入字段，`Default` 里同步加初值：

```rust
    /// 服务端下发的登录接入配置，启动时拉取；拉不到则无法登录
    pub oidc_config: Mutex<Option<OidcConfig>>,
```

```rust
            oidc_config: Mutex::new(None),
```

- [ ] **Step 2: 把 lib.rs 的配置来源改成 AppState**

修改 `apps/desktop/src-tauri/src/lib.rs`：

删除 `pub fn oidc_config()` 整个函数，并把 `injected!` 宏上方的注释改为只描述服务端地址：

```rust
/// 取服务端地址：构建期注入的值优先，缺失时回退到运行时环境变量（开发用）。
/// 它是整条信任链的锚点——客户端凭它决定去哪儿要登录配置，因此只能编译进二进制，
/// 绝不做成用户可改的本地配置。
macro_rules! injected {
    ($key:literal) => {
        option_env!($key)
            .map(str::to_string)
            .or_else(|| std::env::var($key).ok())
    };
}

pub fn server_base_url() -> String {
    injected!("MINTPOP_SERVER_URL").unwrap_or_else(|| "http://127.0.0.1:8080".to_string())
}
```

新增引导函数（放在 `try_silent_login` 之前）：

```rust
/// 启动引导：拉取登录接入配置。成功后才谈得上登录，因此静默登录串在它之后。
async fn bootstrap(app: AppHandle) -> Result<(), String> {
    match auth::bootstrap::fetch_client_config(&server_base_url()).await {
        Ok(cfg) => {
            *app.state::<AppState>().oidc_config.lock().unwrap() = Some(cfg);
            let _ = app.emit("auth://config-ready", ());
            try_silent_login(app.clone()).await;
            Ok(())
        }
        Err(e) => {
            log_error("拉取登录配置失败", &e);
            let reason = e.to_string();
            let _ = app.emit("auth://config-failed", serde_json::json!({ "reason": reason }));
            Err(reason)
        }
    }
}
```

把 `handle_callback` 里的 `let cfg = oidc_config();` 改为从状态取，取不到就直接返回：

```rust
    let Some(cfg) = app.state::<AppState>().oidc_config.lock().unwrap().clone() else {
        log_error("回调到达时尚无登录配置", &"引导未完成");
        return;
    };
```

把 `try_silent_login` 里的 `auth::oidc::refresh(&oidc_config(), &refresh_token)` 改为：

```rust
    let Some(cfg) = app.state::<AppState>().oidc_config.lock().unwrap().clone() else {
        return;
    };

    match auth::oidc::refresh(&cfg, &refresh_token).await {
```

`setup` 里把静默登录那两行换成引导：

```rust
            let handle = app.handle().clone();
            tauri::async_runtime::spawn(async move {
                let _ = bootstrap(handle).await;
            });
```

`invoke_handler` 的列表里补两条：

```rust
            commands::client_config_ready,
            commands::reload_client_config,
```

- [ ] **Step 3: 改命令层**

修改 `apps/desktop/src-tauri/src/commands.rs`：把 `start_login` 里读配置那段换掉，并新增两个命令。`start_login` 改为：

```rust
/// 发起登录：生成 PKCE，打开系统浏览器到 Logto 授权页。
/// 授权完成后由 deep link 回调继续，见 lib.rs 的 handle_callback。
#[tauri::command]
pub fn start_login(app: AppHandle, state: State<'_, AppState>) -> Result<(), String> {
    // 配置由服务端在启动时下发，取不到说明引导没跑通
    let cfg = state
        .oidc_config
        .lock()
        .unwrap()
        .clone()
        .ok_or_else(|| "无法连接服务端，请稍后重试".to_string())?;

    let pair = pkce::generate();
    let st = pkce::random_state();
    let url = oidc::build_authorize_url(&cfg, &pair, &st);

    *state.pending_login.lock().unwrap() = Some(PendingLogin {
        verifier: pair.verifier,
        state: st,
    });

    tauri_plugin_opener::OpenerExt::opener(&app)
        .open_url(url, None::<&str>)
        .map_err(|e| e.to_string())
}
```

在 `auth_status` 之后新增：

```rust
/// 引导配置是否已就绪。前端挂载可能晚于引导完成，光靠事件会漏掉，
/// 因此挂载时先查这个，再监听后续事件。
#[tauri::command]
pub fn client_config_ready(state: State<'_, AppState>) -> bool {
    state.oidc_config.lock().unwrap().is_some()
}

/// 重新拉取引导配置，供登录页的重试按钮调用
#[tauri::command]
pub async fn reload_client_config(app: AppHandle) -> Result<(), String> {
    crate::reload_client_config(app).await
}
```

在 `lib.rs` 里把 `bootstrap` 暴露给命令层——在 `bootstrap` 函数之后补一个薄封装：

```rust
/// 供命令层调用的重试入口
pub async fn reload_client_config(app: AppHandle) -> Result<(), String> {
    bootstrap(app).await
}
```

- [ ] **Step 4: 收缩构建期注入变量**

修改 `apps/desktop/src-tauri/build.rs`，把 `INJECTED_KEYS` 改为只剩一项：

```rust
/// 构建期注入的接入配置。CI 打包时通过环境变量提供，
/// 于是安装包开箱即用，用户机上无需设置任何环境变量。
/// Logto 的 issuer/client_id/api_resource 不在此列——它们由服务端启动时下发。
const INJECTED_KEYS: [&str; 1] = ["MINTPOP_SERVER_URL"];
```

- [ ] **Step 5: 跑测试与 lint 确认通过**

Run: `mise run test-desktop && mise run lint-desktop`
Expected: PASS 且无 clippy warning。若报 `oidc_config` 未定义，说明 Step 2 里还有遗漏的调用点，按编译错误逐个改成从 `AppState` 取。

- [ ] **Step 6: 登录页加"服务端不可达"状态**

修改 `apps/desktop/src/components/Login.vue`，整个 `<script setup>` 与 `<template>` 替换为：

```vue
<script setup lang="ts">
import { onMounted, onUnmounted, ref } from "vue";
import { invoke } from "@tauri-apps/api/core";
import { listen, type UnlistenFn } from "@tauri-apps/api/event";

const error = ref("");
const pending = ref(false);
/** 引导配置是否已就绪。没就绪时登录按钮点了也没用，直接换成重试按钮 */
const configReady = ref(false);
const configError = ref("");
const retrying = ref(false);
const unlisteners: UnlistenFn[] = [];

onMounted(async () => {
  // 引导可能在本组件挂载之前就已完成，光监听事件会漏掉，故先查一次当前状态
  configReady.value = await invoke<boolean>("client_config_ready");

  unlisteners.push(
    await listen("auth://config-ready", () => {
      configReady.value = true;
      configError.value = "";
    }),
  );
  unlisteners.push(
    await listen<{ reason: string }>("auth://config-failed", (event) => {
      configReady.value = false;
      configError.value = event.payload.reason;
    }),
  );
});

onUnmounted(() => {
  unlisteners.forEach((fn) => fn());
});

async function login() {
  pending.value = true;
  error.value = "";
  try {
    await invoke("start_login");
  } catch (e) {
    error.value = String(e);
  } finally {
    pending.value = false;
  }
}

async function retry() {
  retrying.value = true;
  try {
    await invoke("reload_client_config");
  } catch (e) {
    configError.value = String(e);
  } finally {
    retrying.value = false;
  }
}
</script>

<template>
  <div class="login">
    <h1>Mintpop 终端</h1>
    <p class="hint">
      用公司账号登录后，终端会自动接入公司链路并配好 Claude，你无需自行登录 Claude。
    </p>

    <template v-if="configReady">
      <button :disabled="pending" @click="login">
        {{ pending ? "正在打开浏览器…" : "用公司账号登录" }}
      </button>
      <p v-if="error" class="error">{{ error }}</p>
    </template>

    <template v-else>
      <p class="error">无法连接服务端，暂时不能登录。</p>
      <p v-if="configError" class="error">{{ configError }}</p>
      <button :disabled="retrying" @click="retry">
        {{ retrying ? "正在重试…" : "重试" }}
      </button>
    </template>
  </div>
</template>
```

`<style scoped>` 保持原样不动。

- [ ] **Step 7: 手工验收三条路径**

先只启桌面端、不启服务端：

Run: `mise run run-desktop`
Expected: 登录页显示「无法连接服务端，暂时不能登录。」与重试按钮。

另开终端 `mise run run-server` 起服务端后，点重试按钮。
Expected: 切回登录按钮，点击后系统浏览器打开 Logto 授权页。

> ⚠️ deep link 回调在 `tauri dev` 下通常回不来（`mintpop://` 由打包出的 .app 向系统注册），完整登录闭环用 `mise run build-desktop` 的产物验收。

- [ ] **Step 8: 提交**

```bash
git add apps/desktop/src-tauri/src/app_state.rs \
        apps/desktop/src-tauri/src/lib.rs \
        apps/desktop/src-tauri/src/commands.rs \
        apps/desktop/src-tauri/build.rs \
        apps/desktop/src/components/Login.vue
git commit -m "feat: 桌面端改用服务端下发的登录配置"
```

---

### Task 4: 应用标识统一为 ai.mintpop.cli

**Files:**
- Modify: `apps/desktop/src-tauri/tauri.conf.json`
- Modify: `apps/desktop/src-tauri/src/auth/storage.rs`

**Interfaces:**
- Consumes: 无。
- Produces: 无代码接口；影响 macOS/Windows 的应用标识与钥匙串条目所属 service 名。

- [ ] **Step 1: 改 Tauri 应用标识**

修改 `apps/desktop/src-tauri/tauri.conf.json`：

```json
  "identifier": "ai.mintpop.cli",
```

- [ ] **Step 2: 同步钥匙串 service 名**

修改 `apps/desktop/src-tauri/src/auth/storage.rs` 第 4 行：

```rust
const SERVICE: &str = "ai.mintpop.cli";
```

两处必须一致：service 名与应用标识漂移时，排查钥匙串问题会找错条目。

- [ ] **Step 3: 编译确认无误**

Run: `mise run lint-desktop`
Expected: PASS。

- [ ] **Step 4: 提交**

```bash
git add apps/desktop/src-tauri/tauri.conf.json apps/desktop/src-tauri/src/auth/storage.rs
git commit -m "feat: 应用标识统一为 ai.mintpop.cli"
```

---

### Task 5: CI 与运维文档收尾

**Files:**
- Modify: `.github/workflows/release-desktop.yml:126-130`
- Modify: `deploy/README.md`

**Interfaces:**
- Consumes: Task 1~4 的全部改动。
- Produces: 无代码接口。

- [ ] **Step 1: 收缩发版工作流注入的变量**

修改 `.github/workflows/release-desktop.yml` 的「构建安装包」步骤，`env` 只保留一项：

```yaml
      - name: 构建安装包
        env:
          # Logto 的 issuer/client_id/api_resource 由服务端下发，不再编译进包
          MINTPOP_SERVER_URL: ${{ vars.MINTPOP_SERVER_URL }}
        run: mise run build-desktop
```

- [ ] **Step 2: 在部署文档里写清发版顺序与新配置**

在 `deploy/README.md` 末尾追加一节（如已有「发版」相关小节则并入其中）：

```markdown
## 发版顺序（桌面端与服务端）

桌面端的登录配置由服务端的 `GET /api/client-config` 下发，因此**必须先发服务端、再发桌面端**。
顺序反了会让新装的桌面端卡在「无法连接服务端」，因为老服务端上没有这个端点。

服务端上线前确认 `application-prod.yaml` 里已配：

- `spring.security.oauth2.resourceserver.jwt.issuer-uri`（形如 `https://<租户>.logto.app/oidc`）
- `spring.security.oauth2.resourceserver.jwt.audiences[0]`（API Resource 标识）
- `mintpop.client.logto-client-id`（Logto 里桌面端那个原生应用的 App ID）

前两项同时被用于校验 JWT 和下发给客户端，改一处两处同时生效。
```

- [ ] **Step 3: 提交**

```bash
git add .github/workflows/release-desktop.yml deploy/README.md
git commit -m "ci: 桌面端只注入服务端地址，补发版顺序说明"
```

- [ ] **Step 4: 人工收尾（不在仓库内，需人操作）**

到 GitHub 仓库 Settings → Secrets and variables → Actions → Variables，删除三条已废弃的变量：

```bash
gh variable delete MINTPOP_LOGTO_ISSUER
gh variable delete MINTPOP_LOGTO_CLIENT_ID
gh variable delete MINTPOP_API_RESOURCE
gh variable list        # 确认只剩 MINTPOP_SERVER_URL
```

- [ ] **Step 5: 全量验收**

Run: `mise run test-server && mise run test-desktop && mise run lint-desktop`
Expected: 全绿。

再按 spec 第 11 节逐条验收，其中最后一条：

```bash
mise run build-desktop
strings apps/desktop/src-tauri/target/release/mintpop-desktop | grep -i "logto"
```
Expected: 搜不到任何 Logto 租户地址（只可能出现代码里的错误文案，不含 issuer）。

---

## 自查结果

- **spec 覆盖**：第 3 节接口契约 → Task 1；第 4 节服务端设计 → Task 1；第 5 节桌面端设计 → Task 2、Task 3；第 6.1 节应用标识 → Task 4；第 6.2 节本地自测区分 → 无需改代码，由 Global Constraints 中「`MINTPOP_SERVER_URL` 缺省值不得改动」守住，并在 Task 5 的文档里说明；第 7 节错误处理 → Task 3 Step 6（四种表现里「字段缺失」由 Task 2 的 serde 必填字段落实，报错文案走同一条 `config-failed` 通道）；第 8 节构建发版 → Task 5；第 9 节安全 → Task 1 Step 7（精确路径放行）、Task 3 Step 2（信任锚注释）；第 10 节测试 → Task 1 Step 2、Task 2 Step 1；第 11 节验收 → Task 5 Step 5。
- **类型一致性**：`ClientConfigResponse` 的 `logtoIssuer`/`logtoClientId`/`apiResource` 与 Rust 侧 `ClientConfigData` 的 `logto_issuer`/`logto_client_id`/`api_resource`（`rename_all = "camelCase"`）逐字对应；`bootstrap(app)` 与 `reload_client_config(app)` 均返回 `Result<(), String>`；`client_config_ready` 返回 `bool`，前端按 `boolean` 接收。
