# 时区处理（全链路 UTC）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把订阅起止期等全部业务时间从「无时区墙钟」改为「全链路 UTC 绝对时刻」：存储/传输/计算一律 UTC，只在展示时换算成查看者本地时区。

**Architecture:** 后端 `LocalDateTime` 全量换 `Instant` 并注入 `Clock`（DI、可测）；JDBC 连接与 MySQL 会话时区钉死 UTC，`DATETIME` 列语义定为 UTC；API 时间串从无偏移升级为带 `Z` 的 ISO-8601；admin/desktop 前端解析后按浏览器本地时区渲染。已确认无生产数据，直接改 Flyway V1，不写数据迁移。

**Tech Stack:** Spring Boot 4.1 + MyBatis-Plus + MySQL 8.4（Testcontainers 测试）；Vue 3 + Element Plus + vitest（admin）；Tauri + Vue（desktop）。

**Spec:** `docs/superpowers/specs/2026-08-20-时区处理-design.md`

## Global Constraints

- 所有代码注释、文档、提交信息一律**简体中文**；提交信息末尾带 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 命令一律走 mise（在仓库根执行）：`mise run test-server` / `mise run test-admin` / `mise run lint-admin` / `mise run lint-desktop`。
- 新增依赖一律构造器注入（DI），禁止在业务代码里静态取 `Clock`/`Instant.now()`。
- 持久层是 **MyBatis-Plus**（不是 JPA/Hibernate），`Instant` 由 MyBatis 内置 `InstantTypeHandler` 处理，无需注册任何 handler。
- 服务端测试需要 Docker（Testcontainers 起 MySQL 8.4）。
- Flyway V1 被直接修改（已确认数据可丢）：本地若有旧库，执行 `DROP DATABASE lane; CREATE DATABASE lane DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;` 重建；测试容器每次全新，不受影响。

---

### Task 1: 后端 Instant 化 + Clock 注入

**Files:**
- Create: `apps/server/src/main/java/ai/mintpop/lane/config/ClockConfig.java`
- Create: `apps/server/src/test/java/ai/mintpop/lane/dto/SubscriptionDtoTest.java`
- Modify（类型替换，逐文件列出涉及字段）:
  - `apps/server/src/main/java/ai/mintpop/lane/dto/SubscriptionDto.java`（startsAt/endsAt/createdAt/updatedAt + `isActiveAt` 参数）
  - `apps/server/src/main/java/ai/mintpop/lane/dto/UserDto.java`（createdAt/updatedAt）
  - `apps/server/src/main/java/ai/mintpop/lane/dto/ProxyNodeDto.java`（createdAt/updatedAt）
  - `apps/server/src/main/java/ai/mintpop/lane/entity/Subscription.java`（startsAt/endsAt/createdAt/updatedAt）
  - `apps/server/src/main/java/ai/mintpop/lane/entity/User.java`（createdAt/updatedAt）
  - `apps/server/src/main/java/ai/mintpop/lane/entity/ProxyNode.java`（createdAt/updatedAt）
  - `apps/server/src/main/java/ai/mintpop/lane/response/AdminNodeResponse.java`（createdAt/updatedAt）
  - `apps/server/src/main/java/ai/mintpop/lane/response/MeResponse.java`（startsAt/endsAt）
  - `apps/server/src/main/java/ai/mintpop/lane/response/AdminSubscriptionResponse.java`（startsAt/endsAt/createdAt/updatedAt）
  - `apps/server/src/main/java/ai/mintpop/lane/response/LinkConfigResponse.java`（endsAt）
  - `apps/server/src/main/java/ai/mintpop/lane/response/AdminUserResponse.java`（createdAt/updatedAt + 内嵌 `ActiveSubscriptionBrief.endsAt`）
  - `apps/server/src/main/java/ai/mintpop/lane/request/SubscriptionSaveRequest.java`（startsAt/endsAt）
- Modify（逻辑改动）:
  - `apps/server/src/main/java/ai/mintpop/lane/service/LinkServiceImpl.java`（注入 Clock，54/112 两处 now）
  - `apps/server/src/main/java/ai/mintpop/lane/service/AdminUserServiceImpl.java`（注入 Clock，48 行 now）
  - `apps/server/src/main/java/ai/mintpop/lane/controller/AuthController.java`（注入 Clock，84 行 now）
- Test（随类型同步更新）:
  - `apps/server/src/test/java/ai/mintpop/lane/support/DatabaseFixtures.java`
  - `apps/server/src/test/java/ai/mintpop/lane/service/LinkServiceImplTest.java`
  - `apps/server/src/test/java/ai/mintpop/lane/repository/SubscriptionRepositoryTest.java`
  - `apps/server/src/test/java/ai/mintpop/lane/repository/UserRepositoryTest.java`
  - `apps/server/src/test/java/ai/mintpop/lane/controller/AuthControllerTest.java`
  - `apps/server/src/test/java/ai/mintpop/lane/controller/admin/AdminUserControllerTest.java`

**Interfaces:**
- Consumes: 无（首个任务）。
- Produces:
  - `@Bean java.time.Clock clock()`（`Clock.systemUTC()`），业务代码经构造器注入后用 `clock.instant()` 取「现在」。
  - `SubscriptionDto.isActiveAt(Instant now): boolean`（起含止不含语义不变）。
  - 全部实体/DTO/request/response 的时间字段类型为 `java.time.Instant`；HTTP JSON 里序列化为带 `Z` 的 ISO-8601 串（Jackson 3 默认行为，无需配置）。后续任务（前端）依赖这个 `Z` 串契约。

- [ ] **Step 1: 写失败测试——`isActiveAt` 的 Instant 边界判定**

新建 `apps/server/src/test/java/ai/mintpop/lane/dto/SubscriptionDtoTest.java`：

```java
package ai.mintpop.lane.dto;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.time.Instant;

import static org.assertj.core.api.Assertions.assertThat;

class SubscriptionDtoTest {

    @Test
    @DisplayName("在期判定：起含止不含，按 UTC 绝对时刻比较")
    void 起含止不含() {
        SubscriptionDto s = new SubscriptionDto();
        s.setStartsAt(Instant.parse("2026-08-01T00:00:00Z"));
        s.setEndsAt(Instant.parse("2026-09-01T00:00:00Z"));

        assertThat(s.isActiveAt(Instant.parse("2026-07-31T23:59:59Z"))).isFalse();
        assertThat(s.isActiveAt(Instant.parse("2026-08-01T00:00:00Z"))).isTrue();   // 起：含
        assertThat(s.isActiveAt(Instant.parse("2026-08-31T23:59:59Z"))).isTrue();
        assertThat(s.isActiveAt(Instant.parse("2026-09-01T00:00:00Z"))).isFalse();  // 止：不含
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mise run test-server`
Expected: 编译失败——`isActiveAt(java.time.Instant)` 不存在（当前签名是 `LocalDateTime`）。编译红即本步的红。

- [ ] **Step 3: 新建 ClockConfig**

`apps/server/src/main/java/ai/mintpop/lane/config/ClockConfig.java`：

```java
package ai.mintpop.lane.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.time.Clock;

/**
 * 全链路 UTC：业务取「现在」一律注入本 Clock 后 clock.instant()，
 * 不直接调 Instant.now()——测试可用 Clock.fixed 固定时间。
 */
@Configuration
public class ClockConfig {

    @Bean
    public Clock clock() {
        return Clock.systemUTC();
    }
}
```

- [ ] **Step 4: 全量类型替换 `LocalDateTime` → `Instant`**

对上面 Files 列出的 12 个「类型替换」文件逐一执行同一模式：`import java.time.LocalDateTime;` → `import java.time.Instant;`，列出的字段类型 `LocalDateTime` → `Instant`。其中 `SubscriptionDto` 的判定方法改为（方法体逻辑不变，`Instant` 同样有 `isBefore`）：

```java
    /** 在期判定：起含止不含。判定点在拉链路与心跳里，纯查询、无定时任务 */
    public boolean isActiveAt(Instant now) {
        return !now.isBefore(startsAt) && now.isBefore(endsAt);
    }
```

- [ ] **Step 5: 三个 now() 调用点改为注入 Clock**

`LinkServiceImpl`：字段与构造器追加 `Clock`（放最后一个参数位），`import java.time.Clock; import java.time.Instant;` 替换 `LocalDateTime` 导入：

```java
    private final Clock clock;

    public LinkServiceImpl(LinkProperties linkProperties,
                           UserRepository userRepository,
                           ProxyNodeRepository nodeRepository,
                           SubscriptionRepository subscriptionRepository,
                           Clock clock) {
        // 其余赋值不变，追加：
        this.clock = clock;
    }
```

`resolveLink` 里 `LocalDateTime now = LocalDateTime.now();` → `Instant now = clock.instant();`；`heartbeat` 里 `s.isActiveAt(LocalDateTime.now())` → `s.isActiveAt(clock.instant())`。

`AdminUserServiceImpl`：构造器同样追加 `Clock clock` 参数与字段；`page` 里 `LocalDateTime now = LocalDateTime.now();` → `Instant now = clock.instant();`。

`AuthController`：构造器（现有 6 参）追加 `Clock clock`；`me` 端点里 `LocalDateTime now = LocalDateTime.now();` → `Instant now = clock.instant();`。

- [ ] **Step 6: 测试代码随类型同步**

统一替换模式（涉及 6 个测试文件）：
- `import java.time.LocalDateTime;` → `import java.time.Instant;`（用到 `minus/plus` 的加 `import java.time.temporal.ChronoUnit;`）
- `LocalDateTime.now()` → `Instant.now()`；`.minusDays(N)` → `.minus(N, ChronoUnit.DAYS)`；`.plusDays(N)` → `.plus(N, ChronoUnit.DAYS)`
- `SubscriptionRepositoryTest` 73 行 `LocalDateTime.of(2026, 8, 19, 12, 0)` → `Instant.parse("2026-08-19T12:00:00Z")`
- `DatabaseFixtures.建订阅` / `建可用用户` 的参数类型 `LocalDateTime` → `Instant`

`LinkServiceImplTest` 额外改动——用固定时钟消除对真实时间的依赖：

```java
    /** 全类统一的「现在」，service 用固定时钟构造，判定完全确定 */
    private static final Instant NOW = Instant.parse("2026-08-19T12:00:00Z");
```

`在期订阅`/`过期订阅` 两个辅助方法里的 `LocalDateTime now = LocalDateTime.now();` 改为直接用 `NOW`（如 `NOW.minus(1, ChronoUnit.DAYS)`）；`准备()` 里构造 service 时追加参数 `Clock.fixed(NOW, ZoneOffset.UTC)`（`import java.time.Clock; import java.time.ZoneOffset;`）。类中其余 `LocalDateTime.now()` 一律换成 `NOW`。

`AuthControllerTest` 的 `me返回订阅概览` 用例追加 API 契约断言（`import static org.hamcrest.Matchers.matchesPattern;`）：

```java
                // 时间字段必须是带 Z 的 UTC 绝对时刻串，这是与前端的契约
                .andExpect(jsonPath("$.data.subscriptions[0].endsAt", matchesPattern(".+Z$")))
```

- [ ] **Step 7: 跑全部服务端测试**

Run: `mise run test-server`
Expected: 全绿（含新加的 `SubscriptionDtoTest` 与 `Z` 后缀断言）。

- [ ] **Step 8: 提交**

```bash
git add apps/server
git commit -m "refactor: 业务时间全量改 Instant 并注入 Clock，API 输出带 Z 的 UTC 串

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 连接与会话时区钉 UTC + SQL 与 V1 语义

**Files:**
- Modify: `apps/server/config/application.yml:16`、`apps/server/config/application.example.yml:16`（JDBC URL）
- Modify: `apps/server/src/test/java/ai/mintpop/lane/support/MysqlTestBase.java`、`apps/server/src/test/java/ai/mintpop/lane/controller/DesktopLoginControllerIT.java`（容器 URL 参数）
- Modify: `apps/server/src/main/java/ai/mintpop/lane/repository/MybatisUserRepository.java:74,77`（NOW() → UTC_TIMESTAMP()）
- Modify: `apps/server/src/main/resources/db/migration/V1__init_schema.sql`（时间列注释补 UTC）
- Test: `apps/server/src/test/java/ai/mintpop/lane/repository/SubscriptionRepositoryTest.java`（新增落库字面值断言）

**Interfaces:**
- Consumes: Task 1 的 `Instant` 字段与 `DatabaseFixtures.建订阅(Long, AgentType, String, Instant, Instant, String)`。
- Produces: 「`DATETIME` 列字面值 = UTC 墙钟」的存储契约；JDBC URL 固定参数 `connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true`（后者把 MySQL 会话时区钉 UTC，使 DDL 的 `DEFAULT CURRENT_TIMESTAMP` 自动填充值同为 UTC）。

- [ ] **Step 1: 写失败测试——写入的 Instant 在库里必须是 UTC 字面值**

在 `SubscriptionRepositoryTest` 新增用例（`@Autowired JdbcTemplate jdbcTemplate;` 注入字段，`import org.springframework.jdbc.core.JdbcTemplate;`；建订阅所需的 userId 沿用该类相邻用例的既有写法）：

```java
    @Test
    @DisplayName("落库语义：写入的 Instant 在 DATETIME 列里的字面值就是 UTC 墙钟")
    void 落库字面值为UTC墙钟() {
        Long id = fixtures.建订阅(userId, AgentType.CLAUDE, "UTC 落库",
                Instant.parse("2026-08-01T00:00:00Z"), Instant.parse("2026-09-01T00:00:00Z"), "cred");
        String literal = jdbcTemplate.queryForObject(
                "SELECT DATE_FORMAT(starts_at, '%Y-%m-%dT%H:%i:%s') FROM subscription WHERE id = ?",
                String.class, id);
        // 若 JVM 时区渗入编解码（driver 默认 connectionTimeZone=LOCAL），这里会差出本机时区的偏移
        assertThat(literal).isEqualTo("2026-08-01T00:00:00");
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mise run test-server`
Expected: 新用例 FAIL——本机 JVM 时区非 UTC（如 America/Los_Angeles）时字面值差出偏移量。（若在 JVM 时区恰为 UTC 的机器上跑会直接绿，属已知现象，继续后续步骤即可。）

- [ ] **Step 3: JDBC URL 与测试容器钉 UTC**

`application.yml` 与 `application.example.yml` 的 URL 改为（仅时区参数变化）：

```yaml
    url: "jdbc:mysql://127.0.0.1:3306/lane?useUnicode=true&characterEncoding=utf8&connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true"
```

`MysqlTestBase` 与 `DesktopLoginControllerIT` 给容器加同样的 URL 参数。注意：两处字段声明都是**裸类型** `MySQLContainer`，链式 `new MySQLContainer(...).withUrlParam(...)` 经类型擦除后返回 `JdbcDatabaseContainer`、赋值编译不过——所以**不改字段声明**，在 `start()` 之前补一句调用。`MysqlTestBase` 的 static 块改为：

```java
    static {
        // 与生产 JDBC URL 的时区参数一致：编解码与会话时区都钉 UTC
        MYSQL.withUrlParam("connectionTimeZone", "UTC")
             .withUrlParam("forceConnectionTimeZoneToSession", "true");
        MYSQL.start();
    }
```

`DesktopLoginControllerIT` 里对它自己的 `MYSQL` 容器照同一模式：在其启动语句之前加同样两个 `withUrlParam` 调用（该类如何启动容器以文件现状为准，参数调用放在启动之前即可）。

- [ ] **Step 4: 在期过滤 SQL 改 UTC_TIMESTAMP()**

`MybatisUserRepository` 两处 exists/notExists 子查询（74、77 行）里的 `NOW()` 全部改为 `UTC_TIMESTAMP()`——与 UTC 存储语义配对，不依赖会话时区。

- [ ] **Step 5: V1 时间列注释补 UTC 语义**

`V1__init_schema.sql` 逐列改 COMMENT（列定义与默认值不动）：
- 三张表的 `created_at` → `'创建时间（UTC）'`（app_user 表保留原括注：`'创建时间（首次登录时间，UTC）'`）
- 三张表的 `updated_at` → `'更新时间（UTC）'`
- `starts_at` → `'服务起期（含，UTC）'`
- `ends_at` → `'服务止期（不含，UTC）；在期判定为 starts_at <= now < ends_at（now 取 UTC），纯查询、无定时任务'`

- [ ] **Step 6: 跑全部服务端测试**

Run: `mise run test-server`
Expected: 全绿，含 Step 1 新用例（容器 URL 参数生效后编解码钉死 UTC，与 JVM 时区无关）。

- [ ] **Step 7: 提交**

```bash
git add apps/server
git commit -m "fix: JDBC 与 MySQL 会话时区钉死 UTC，DATETIME 列语义定为 UTC

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: admin 展示按本地时区渲染

**Files:**
- Modify: `apps/admin/src/utils/format.ts`（formatDateTime）
- Test: `apps/admin/src/utils/format.spec.ts`

**Interfaces:**
- Consumes: Task 1 的 API 契约——服务端时间串带 `Z`（如 `2026-08-18T02:20:30Z`）。
- Produces: `formatDateTime(value?: string | null): string`——解析带 `Z` 串、按浏览器本地时区渲染 `YYYY-MM-DD HH:mm`；签名不变，所有展示处（列表、抽屉）无需改动。

- [ ] **Step 1: 改写测试为本地时区语义**

`format.spec.ts` 的 `formatDateTime` describe 块整体替换（其余 describe 不动）：

```ts
// 钉死本进程时区，让「按本地时区渲染」可断言（Node 在 POSIX 上支持运行中生效）
process.env.TZ = "Asia/Shanghai";

describe("formatDateTime", () => {
  it("把服务端的 UTC 时间串按本地时区渲染到分钟", () => {
    // UTC 02:20 = 北京时间 10:20
    expect(formatDateTime("2026-08-18T02:20:30Z")).toBe("2026-08-18 10:20");
  });

  it("跨日换算正确——UTC 傍晚是北京次日凌晨", () => {
    expect(formatDateTime("2026-08-18T17:00:00Z")).toBe("2026-08-19 01:00");
  });

  it("空值显示占位符，不显示 undefined", () => {
    expect(formatDateTime(null)).toBe("—");
    expect(formatDateTime(undefined)).toBe("—");
    expect(formatDateTime("")).toBe("—");
  });

  it("解析不了的串显示占位符，不显示 Invalid Date", () => {
    expect(formatDateTime("不是时间")).toBe("—");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mise run test-admin`
Expected: `formatDateTime` 相关用例 FAIL（现实现裁字符串，输出 `2026-08-18 02:20` 且不换算）。

- [ ] **Step 3: 实现本地时区渲染**

`format.ts` 里 `formatDateTime` 整体替换（含注释）：

```ts
/**
 * 服务端时间一律是带 Z 的 UTC 绝对时刻串（如 `2026-08-18T02:20:30Z`）。
 * 带 Z 的串交给 Date 解析没有歧义，这里换算成浏览器本地时区渲染到分钟——
 * 谁在看就按谁的时区显示，与部署环境时区无关。
 */
export function formatDateTime(value?: string | null): string {
  if (!value) {
    return PLACEHOLDER;
  }
  const d = new Date(value);
  if (Number.isNaN(d.getTime())) {
    return PLACEHOLDER;
  }
  const pad = (n: number) => String(n).padStart(2, "0");
  return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `mise run test-admin`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add apps/admin/src/utils/format.ts apps/admin/src/utils/format.spec.ts
git commit -m "feat: 管理端时间展示改为按浏览器本地时区渲染 UTC 串

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: admin 订阅表单 Date 化

**Files:**
- Modify: `apps/admin/src/utils/subscriptionForm.ts`
- Modify: `apps/admin/src/components/SubscriptionDrawer.vue`（两个 el-date-picker 与提示文案）
- Test: `apps/admin/src/utils/subscriptionForm.spec.ts`

**Interfaces:**
- Consumes: Task 1 的 API 契约（响应带 `Z` 串；`SubscriptionSaveRequest.startsAt/endsAt` 仍是 `string`，接受任意 ISO UTC 串——含 `.000Z` 毫秒，服务端 `Instant` 反序列化兼容）。
- Produces: `SubscriptionFormModel.startsAt/endsAt: Date | null`（el-date-picker 直接绑定 Date）；`buildSubscriptionPayload` 输出 `toISOString()` 的 UTC 串；`validateSubscriptionForm` 空值与先后校验语义不变。

- [ ] **Step 1: 改写测试为 Date 模型**

`subscriptionForm.spec.ts` 整文件替换：

```ts
import { describe, expect, it } from "vitest";
import type { AdminSubscriptionResponse } from "../api/types";
import {
  buildSubscriptionPayload,
  emptySubscriptionForm,
  subscriptionToForm,
  validateSubscriptionForm,
} from "./subscriptionForm";

const 样例: AdminSubscriptionResponse = {
  id: 3,
  userId: 7,
  agentType: "CLAUDE",
  name: "Claude 席位 1",
  startsAt: "2026-08-01T00:00:00Z",
  endsAt: "2026-09-01T00:00:00Z",
  hasCredential: true,
  remark: "线下收款",
  createdAt: "2026-08-01T00:00:00Z",
  updatedAt: "2026-08-01T00:00:00Z",
};

describe("subscriptionForm", () => {
  it("空表单默认 CLAUDE、起止期为 null、凭据为空", () => {
    const form = emptySubscriptionForm();
    expect(form.id).toBeNull();
    expect(form.agentType).toBe("CLAUDE");
    expect(form.startsAt).toBeNull();
    expect(form.endsAt).toBeNull();
    expect(form.credential).toBe("");
  });

  it("回填把 UTC 串解析成 Date，不含凭据", () => {
    const form = subscriptionToForm(样例);
    expect(form.startsAt?.toISOString()).toBe("2026-08-01T00:00:00.000Z");
    expect(form.credential).toBe("");
  });

  it("校验：套餐名必填、超长拒绝、止期必须晚于起期（相等也拒绝）", () => {
    const form = emptySubscriptionForm();
    form.startsAt = new Date("2026-09-01T00:00:00Z");
    form.endsAt = new Date("2026-09-01T00:00:00Z");
    const errors = validateSubscriptionForm(form);
    expect(errors).toContain("套餐名不能为空");
    expect(errors).toContain("止期必须晚于起期");

    form.name = "长".repeat(65);
    expect(validateSubscriptionForm(form)).toContain("套餐名最长 64 个字符");
  });

  it("合法表单生成 UTC 串入参——提交往返不漂移", () => {
    const form = subscriptionToForm(样例);
    form.name = "  Claude 席位 1  ";
    expect(validateSubscriptionForm(form)).toEqual([]);
    const payload = buildSubscriptionPayload(form);
    expect(payload.name).toBe("Claude 席位 1");
    expect(payload.startsAt).toBe("2026-08-01T00:00:00.000Z");
    expect(payload.endsAt).toBe("2026-09-01T00:00:00.000Z");
  });

  it("未通过校验就构造入参是编程错误，直接抛出", () => {
    expect(() => buildSubscriptionPayload(emptySubscriptionForm())).toThrow();
  });

  it("未知 agentType 回填时保留原值以免误改", () => {
    const form = subscriptionToForm({ ...样例, agentType: "FUTURE_AGENT" });
    expect(form.agentType).toBe("FUTURE_AGENT");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mise run test-admin`
Expected: FAIL（现模型是 string，`toISOString` 不存在、空表单起止期是 `""` 非 `null`）。

- [ ] **Step 3: 实现 Date 模型**

`subscriptionForm.ts` 中除 `import` 行外整体替换：

```ts
/** 表单模型。agentType 用 string：回填未知类型时保留原值，避免打开即误改 */
export interface SubscriptionFormModel {
  id: number | null;
  agentType: string;
  name: string;
  /** el-date-picker 直接绑定 Date（按管理员本地时区取值）；null = 未填 */
  startsAt: Date | null;
  endsAt: Date | null;
  /** 留空表示沿用原凭据 */
  credential: string;
  remark: string;
}

export function emptySubscriptionForm(): SubscriptionFormModel {
  return {
    id: null,
    agentType: "CLAUDE",
    name: "",
    startsAt: null,
    endsAt: null,
    credential: "",
    remark: "",
  };
}

export function subscriptionToForm(s: AdminSubscriptionResponse): SubscriptionFormModel {
  return {
    id: s.id,
    agentType: s.agentType,
    name: s.name,
    // 服务端给的是带 Z 的 UTC 串，解析成 Date 后由控件按本地时区回显
    startsAt: new Date(s.startsAt),
    endsAt: new Date(s.endsAt),
    // 服务端不回传凭据，回填一律为空；提交时空串即表示不修改
    credential: "",
    remark: s.remark ?? "",
  };
}

export function validateSubscriptionForm(form: SubscriptionFormModel): string[] {
  const errors: string[] = [];
  const name = form.name.trim();
  if (!name) {
    errors.push("套餐名不能为空");
  } else if (name.length > 64) {
    errors.push("套餐名最长 64 个字符");
  }
  if (!form.startsAt) {
    errors.push("起期不能为空");
  }
  if (!form.endsAt) {
    errors.push("止期不能为空");
  }
  // 绝对时刻直接比 epoch；与服务端「止期必须晚于起期」判定一致
  if (form.startsAt && form.endsAt && form.endsAt.getTime() <= form.startsAt.getTime()) {
    errors.push("止期必须晚于起期");
  }
  return errors;
}

export function buildSubscriptionPayload(form: SubscriptionFormModel): SubscriptionSaveRequest {
  if (!form.startsAt || !form.endsAt) {
    // 调用契约：先过 validateSubscriptionForm；走到这里是编程错误
    throw new Error("起止期未填，先通过表单校验再构造入参");
  }
  return {
    agentType: form.agentType as SubscriptionSaveRequest["agentType"],
    name: form.name.trim(),
    // toISOString 天然输出 UTC（带 Z），提交即绝对时刻
    startsAt: form.startsAt.toISOString(),
    endsAt: form.endsAt.toISOString(),
    // 空串 = 沿用原值，服务端按空白转 null 处理
    credential: form.credential.trim(),
    remark: form.remark.trim(),
  };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `mise run test-admin`
Expected: PASS。

- [ ] **Step 5: 抽屉控件与提示文案**

`SubscriptionDrawer.vue`：
1. 两个 `el-date-picker`（起期 176 行、止期 184 行附近）**删掉 `value-format="YYYY-MM-DDTHH:mm:ss"` 一行**（不设 value-format 时 v-model 即 Date 对象，与新模型对上）。
2. `<script setup>` 里加一行：

```ts
/** 管理员当前浏览器时区，标在表单里免得填的人心里没数 */
const 本地时区 = Intl.DateTimeFormat().resolvedOptions().timeZone;
```

3. 提示文案（190-194 行附近）整块替换：

```vue
      <el-form-item>
        <span style="color: var(--el-text-color-secondary); font-size: 12px">
          起止期按你的本地时区（{{ 本地时区 }}）填写，保存为绝对时刻，各端按各自时区显示
        </span>
      </el-form-item>
```

- [ ] **Step 6: 全量校验**

Run: `mise run test-admin`，再 `mise run lint-admin`
Expected: 测试全绿；eslint 与 vue-tsc 无报错（模型改 Date 后若有残留 string 赋值，vue-tsc 会在此拦住）。

- [ ] **Step 7: 提交**

```bash
git add apps/admin
git commit -m "feat: 订阅起止期表单按本地时区填写、以 UTC 绝对时刻提交

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: desktop 到期日按本地时区展示

**Files:**
- Modify: `apps/desktop/src/App.vue:88-91`（day 函数）
- Modify: `apps/desktop/src/components/SessionWizard.vue:74-77`（day 函数）

**Interfaces:**
- Consumes: Task 1 的 API 契约——`endsAt` 为带 `Z` 的 UTC 串。
- Produces: 无（纯展示；desktop 前端无单测设施，靠 vue-tsc 与 Task 6 回归把关）。

- [ ] **Step 1: 两处 day() 改为本地时区取日期**

两个文件中的 `day` 函数用同一实现替换（连同注释）：

```ts
/** 止期按本地时区展示日期部分（服务端给的是带 Z 的 UTC 绝对时刻串） */
function day(iso: string) {
  const d = new Date(iso);
  const pad = (n: number) => String(n).padStart(2, "0");
  return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;
}
```

- [ ] **Step 2: 类型与风格校验**

Run: `mise run lint-desktop`
Expected: clippy 与 vue-tsc 均无报错。

- [ ] **Step 3: 提交**

```bash
git add apps/desktop/src
git commit -m "feat: 桌面端到期日按用户本地时区展示

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: 部署与文档收尾 + 全量回归

**Files:**
- Modify: `docker-compose.yml:12-13`（TZ 注释）
- Modify: `README.md:28`（首次部署第 3 步）、`README.md:191`（可调参数表 TZ 行）

**Interfaces:**
- Consumes: 前五个任务全部完成（文档描述的是新行为）。
- Produces: 无（文档）。

- [ ] **Step 1: compose 的 TZ 注释降级**

`docker-compose.yml` 12 行注释替换：

```yaml
      # 仅影响容器日志时间戳；业务时间全链路 UTC 存取，与本变量无关
      TZ: ${TZ:-UTC}
```

- [ ] **Step 2: README 两处同步**

1. 第 28 行（首次部署第 3 步）删去句尾「建议至少设 `TZ`（如 `Asia/Shanghai`）——订阅起止期按服务端时区判定。」整句，该步以「全用默认值就跳过这一步。」收尾。
2. 第 191 行可调参数表 `TZ` 行替换为：

```markdown
| `TZ` | `UTC` | 服务端容器时区，仅影响日志时间显示。业务时间全链路按 UTC 存取、按查看者本地时区显示，与本变量无关 |
```

- [ ] **Step 3: 全量回归**

依次 Run（任一失败即修复后重跑）：
- `mise run test-server`
- `mise run test-admin`
- `mise run lint-admin`
- `mise run lint-desktop`
Expected: 全部通过。

- [ ] **Step 4: 提交**

```bash
git add docker-compose.yml README.md
git commit -m "docs: TZ 降级为仅影响日志，部署文档同步全链路 UTC 语义

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
