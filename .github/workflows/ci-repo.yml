name: CI Repo

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-repo-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: 质量门禁
    uses: ./.github/workflows/quality.yml
